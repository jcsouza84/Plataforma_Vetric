# ═══════════════════════════════════════════════════════════
# 🔒 VETRIC - GitIgnore
# Arquivos que NUNCA devem ir para o GitHub
# ═══════════════════════════════════════════════════════════

# ┌─────────────────────────────────────────────────────────┐
# │ SEGURANÇA - ARQUIVOS SENSÍVEIS (CRÍTICO!)              │
# └─────────────────────────────────────────────────────────┘
.env
.env.local
.env.*.local
.env.production
.env.development
*.pem
*.key
*.cert
*.crt

# Arquivos de sessão e tokens
session-info.json
cookies.json
token.txt

# ┌─────────────────────────────────────────────────────────┐
# │ NODE.JS                                                  │
# └─────────────────────────────────────────────────────────┘
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
.pnpm-debug.log*

# Build outputs
dist/
build/
*.tsbuildinfo

# ┌─────────────────────────────────────────────────────────┐
# │ LOGS E DADOS TEMPORÁRIOS                                │
# └─────────────────────────────────────────────────────────┘
logs/
*.log
combined.log
error.log
raw-messages/

# PM2
.pm2/
pids/
*.pid
*.seed
*.pid.lock

# ┌─────────────────────────────────────────────────────────┐
# │ UPLOADS E ARQUIVOS DE USUÁRIO                           │
# └─────────────────────────────────────────────────────────┘
uploads/
backups/
*.pdf
*.xlsx
*.xls
*.csv
relatorios/

# Exceções: Manter exemplos
!uploads/.gitkeep
!backups/.gitkeep

# ┌─────────────────────────────────────────────────────────┐
# │ BANCO DE DADOS                                           │
# └─────────────────────────────────────────────────────────┘
*.sqlite
*.sqlite3
*.db
*.sql

# PostgreSQL
pgdata/

# ┌─────────────────────────────────────────────────────────┐
# │ SISTEMA OPERACIONAL                                      │
# └─────────────────────────────────────────────────────────┘
# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Linux
*~
.directory

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# ┌─────────────────────────────────────────────────────────┐
# │ IDEs E EDITORES                                          │
# └─────────────────────────────────────────────────────────┘
# VSCode / Cursor
.vscode/
.cursor/
*.code-workspace

# JetBrains
.idea/
*.iml
*.iws
*.ipr

# Vim
[._]*.s[a-v][a-z]
[._]*.sw[a-p]
[._]s[a-rt-v][a-z]
[._]ss[a-gi-z]
[._]sw[a-p]

# ┌─────────────────────────────────────────────────────────┐
# │ TESTES E COVERAGE                                        │
# └─────────────────────────────────────────────────────────┘
coverage/
*.lcov
.nyc_output/
test-results/
test-output.log

# ┌─────────────────────────────────────────────────────────┐
# │ CACHE E TEMPORÁRIOS                                      │
# └─────────────────────────────────────────────────────────┘
.cache/
.temp/
.tmp/
*.tmp
*.bak
*.swp
*.swo

# ┌─────────────────────────────────────────────────────────┐
# │ OUTROS                                                   │
# └─────────────────────────────────────────────────────────┘
# Chargers data (arquivo gerado)
chargers.json

# Lock files (manter apenas package-lock.json no repo)
yarn.lock
pnpm-lock.yaml

# TypeScript
*.tsbuildinfo

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Output of 'npm pack'
*.tgz

# dotenv environment variable files
.env.test

# Archives
*.zip
*.tar.gz
*.rar

# ═══════════════════════════════════════════════════════════
# FIM DO .gitignore
# ═══════════════════════════════════════════════════════════
